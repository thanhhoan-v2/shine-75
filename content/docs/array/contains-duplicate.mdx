---
title: Contains Duplicate
description: Given an integer array nums, return true if any value appears at least twice in the array, and return false if every element is distinct.
difficulty: Easy
timeLimit: 15 mins
week: 1
tags:
  - Array
  - Hash Table
  - Sorting
---

```js title="Example"
Input: nums = [1, 2, 3, 1]
Output: true
Explanation: The value 1 appears twice in the array.

Input: nums = [1, 2, 3, 4]
Output: false
Explanation: All elements are distinct.
```

## Approach

1. **Hash Set Method**: Use a hash set to track seen numbers.
2. **Iterate Through Array**: Iterate through the array once.
3. **Check for Duplicates**: If a number is already in the set, return true.
4. **Add to Set**: Otherwise, add the number to the set.
5. **Return False**: Return false if no duplicates are found.

## Solution

- Time Complexity: `O(n)`
  > Where n is the length of the array. We iterate through the array once.
- Space Complexity: `O(n)`
  > We use a hash set to store seen numbers, which can grow up to n elements.

```js tab="JavaScript"
/**
 * @param {number[]} nums
 * @return {boolean}
 */
var containsDuplicate = function(nums) {
    const seen = new Set();
    
    for (let num of nums) {
        if (seen.has(num)) {
            return true;
        }
        seen.add(num);
    }
    
    return false;
};
```

```java tab="Java"
class Solution {
    public boolean containsDuplicate(int[] nums) {
        Set<Integer> seen = new HashSet<>();
        
        for (int num : nums) {
            if (seen.contains(num)) {
                return true;
            }
            seen.add(num);
        }
        
        return false;
    }
}
```
